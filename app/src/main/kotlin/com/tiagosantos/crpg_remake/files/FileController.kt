package com.tiagosantos.crpg_remake.files

import com.google.gson.Gson
import com.tiagosantos.common.ui.model.*
import com.tiagosantos.common.ui.utils.Constants
import java.io.File
import java.util.ArrayList

object FileController {
    /*
    val ctx = application.applicationContext
    val gson = Gson()
    val filename = Constants.REMINDER_FILENAME
    val fullFilename = ctx.filesDir.toString() + Constants.SLASH + filename
    val file = File(fullFilename)
    val fileExists = file.exists()
    val messagesList = ArrayList<Reminder>()
    val isNewFileCreated : Boolean = file.createNewFile()

     */
}
